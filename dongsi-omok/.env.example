# No production URL is required for Vercel deployment.
# The browser connects to Socket.IO on the same origin at /api/socket.
# Local development uses Vite -> localhost:3001 proxy automatically.
PORT=3001
